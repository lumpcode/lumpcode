/** @deprecated Import from `../backlog` instead. Re-exports for backward compatibility. */
export {
    backlogPaths as abstractionBacklogPaths,
    loadAbstractionBacklogContexts,
    makeAbstractionBacklogContextListFn,
    type AbstractionBacklogItem,
    type DoneBacklogItem as DoneAbstractionBacklogItem,
    type AbstractionBacklogContextVariables,
    type MakeBacklogContextListFnOptions as MakeAbstractionBacklogContextListFnOptions,
} from '../backlog';
