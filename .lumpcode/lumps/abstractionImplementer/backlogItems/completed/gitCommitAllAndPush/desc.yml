name: gitCommitAllAndPush
task: >-
  Extract repeated git fixture helper (git add -A, try commit then commit
  --allow-empty on failure, push to origin) into util `gitCommitAllAndPush`;
  refactor identical `gitCommitAll` in `utils/validateDaemonLaunch/unit.test.ts`
  and `utils/discoverDedicatedLumpsForScanBranch/unit.test.ts`,
  `commitAndPushMain` and `gitCommitIntegrationBranch` in
  `e2e/harness/createE2eProject.ts`, and the try/catch commit block in
  `testing/multiBranchFixtures.ts` `createIntegrationBranch`.
priority: 6
dependsOn:
  - execGit
completedAt: '2026-08-10T09:09:42.810Z'
