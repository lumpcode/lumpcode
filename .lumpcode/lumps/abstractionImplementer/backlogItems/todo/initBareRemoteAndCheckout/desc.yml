name: initBareRemoteAndCheckout
task: >-
  Extract repeated bare-remote + local-repo + push fixture setup (`init --bare`,
  local bootstrap, `remote add origin`, `push -u origin main`) into util
  `initBareRemoteAndCheckout` built on `initLocalGitRepo`/`execGit`; promote the
  helper from `testing/multiBranchFixtures.ts`, remove private
  `initRepoWithRemote`, and refactor ~12 unit-test / start-helper copies that
  still inline the seven-command sequence.
priority: 7
dependsOn:
  - execGit
  - initLocalGitRepo
