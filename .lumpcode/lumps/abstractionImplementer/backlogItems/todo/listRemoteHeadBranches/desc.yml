name: listRemoteHeadBranches
task: >-
  Extract repeated `git ls-remote --heads origin` invocation plus stdout parsing
  (split ref lines, strip `refs/heads/`, filter by branch prefix, dedupe) into util
  `listRemoteHeadBranches`; refactor `commands/clean/main.ts` (`parseRefsFromLsRemote`,
  `discoverRemoteBranches`) and `utils/countOpenLumpBranches/main.ts` (inline parse loop).
priority: 1
