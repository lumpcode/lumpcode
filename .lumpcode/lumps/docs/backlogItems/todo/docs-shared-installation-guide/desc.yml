# yaml-language-server: $schema=https://lumpcode.com/schemas/backlogDesc.schema.json
name: docs-shared-installation-guide
workflow: [manualReq, directImpl]
task: >-
  Quick-start DOCS guide for shared laptop rehearsal (in-place run on the current
  branch; dirty or detached HEAD fails; start is dedicated-only). Do not describe
  a project copy or "never touches this checkout". Position shared as rehearsal
  before a dedicated worker; the worker still cuts lump/… branches.
priority: 4
