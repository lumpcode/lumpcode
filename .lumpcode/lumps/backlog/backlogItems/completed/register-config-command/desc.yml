name: register-config-command
workflow: [req]
task: >-
  Bugfix: ensure the command specified in lump/step `command` is registered (or
  otherwise resolved) so runs do not fail when that command tag or module was
  not previously registered.
priority: 10
completedAt: '2026-08-25T09:11:43.779Z'
