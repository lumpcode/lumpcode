name: setup-resume-and-worker
workflow:
  - manualReq
  - testImpl
  - impl
task: >-
  Re-running `setup` on a clone that already has `.lumpcode/` writes this
  machine’s `local.json` without rewriting `project.json` or the lump stub, and
  a dedicated clone can start the unfiltered worker.
priority: 2
dependsOn:
  - setup-first-pr-drive
completedAt: '2026-09-16T16:41:30.381Z'
