name: register-config-command
manualReq: true
workflow: directImpl
task: >-
  Bugfix: ensure the command specified in lump/step `command` is registered
  (or otherwise resolved) so runs do not fail when that command tag or module
  was not previously registered.
priority: 10
