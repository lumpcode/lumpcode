name: docs-dedicated-installation-guide
workflow: directImpl
task: >-
  Quick-start DOCS guide for dedicated mode (the most powerful / production-intended
  way to use Lumpcode). Cover roughly: (1) the machine needs only a repo clone with
  git access and an agent CLI; (2) run project-setup if needed, then set local.json
  to dedicated; (3) explain worktree vs checkout — recommend worktree when the repo
  can use it, and explain maxParallelRun with worktree; (4) launch the global daemon;
  (5) day-to-day on the work repo you only add/update lumps and the global daemon
  picks them up. Rename from the old "distant" idea name as needed.
priority: 17
