# yaml-language-server: $schema=https://lumpcode.com/schemas/featureBacklogDesc.schema.json
name: lumpcode-setup
workflow: [manualReq, testImpl, impl]
manual: true
task: >-
  Interactive `lumpcode setup` command that walks a user through shared or
  dedicated mode, then to a first lump, and finishes when that lump has pushed
  a branch they can open as a PR. Easy to set up yet powerful; not only today's
  flag-based `project-setup` scaffold. See requirements.md.
priority: 1
