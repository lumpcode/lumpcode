desc: "A contextListJson value with no placeholder matches that relative path exactly and names the context from the file basename."
files:
  packages/apps/cli/src/utils/makeGetContextListFnFromTemplate/main.ts:
    desc: "After normalize, no { / $modifier{ tokens → exact-equality match; name is basename minus the final extension if legal. Else keep extractPattern."
    "-": 8
    "+": 35
    symbols:
      makeGetContextListFnFromTemplate: update
  packages/apps/cli/src/utils/makeGetContextListFnFromTemplate/unit.test.ts:
    desc: "README.md → README; nested legal basename; illegal derived name emits nothing; {NAME}.md unchanged."
    "-": 0
    "+": 70
  packages/apps/cli/DOCS/lump-config.md:
    desc: "No placeholder = exact relative path (README.md → context README)."
    "-": 4
    "+": 16
  packages/apps/website/content/docs/author/contexts.md:
    desc: "Same exact-path rule next to the path-pattern docs."
    "-": 4
    "+": 14
