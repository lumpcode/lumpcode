# yaml-language-server: $schema=https://lumpcode.com/schemas/featureBacklogDesc.schema.json
name: local-status-dashboard-mvp
workflow: [manualReq, testImpl, impl]
manual: true
task: >-
  Parked. Local web board of git context status (todo / branchPushed /
  finished) with the remote as source of truth. Not this slice: the worker
  operator UI is `worker-dashboard-readonly` / `worker-dashboard-controls`
  (live daemon snapshot, not campaign progress). Unpark after that path ships
  if a git board is still wanted. Complements parked `cli-status-dashboard`.
priority: 31
dependsOn:
  - worker-dashboard-readonly
