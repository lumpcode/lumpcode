name: 02-respawn-strategy-from-meta
manualReq: true
workflow: directImpl
priority: 3
task: >-
  spawnDesiredDaemon must take workspaceStrategy from that daemon's meta, not a
  live local.json read. Missing/invalid meta skips spawn.
