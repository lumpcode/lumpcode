name: recipes-manual-req-default
manualReq: true
workflow: directImpl
task: >-
  In recipes (e.g. featureBacklog / backlog), default manualReq to true so
  requirements are authored by a human unless explicitly opted out.
priority: 11
