# yaml-language-server: $schema=https://lumpcode.com/schemas/featureBacklogDesc.schema.json
name: cli-status-dashboard
workflow: [manualReq, testImpl, impl]
manual: true
task: >-
  Parked. Unified terminal view of git campaign status (todo / branchPushed /
  finished, how many left) plus daemon state — one `lumpcode status` so
  operators do not bounce between daemon-status, lump-status, and
  context-status. Not this slice: live "what is running / next tick" is
  `daemon-status-snapshot` then the worker dashboard. Unpark after that path
  ships. Complements parked `local-status-dashboard-mvp` (same git board in a
  UI). File-shaped; not a database. Supersedes IDEAS unified-status-command.
priority: 30
dependsOn:
  - daemon-status-snapshot
