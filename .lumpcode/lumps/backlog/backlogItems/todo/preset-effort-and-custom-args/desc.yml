name: preset-effort-and-custom-args
manualReq: true
task: >-
  Add effort (and similar) options to agent presets, plus a custom args list
  so operators can pass extra CLI flags through lumpVariables/stepVariables.
priority: 8
